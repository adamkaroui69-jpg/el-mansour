<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ============================================ -->
    <!-- PALETTE DASHBOARD FINANCIER MODERNE          -->
    <!-- Style Bancaire/Fintech Professionnel         -->
    <!-- ============================================ -->
    
    <!-- Couleurs d'Accent (Bleu Professionnel - NOUVEAU DESIGN SYSTEM) -->
    <Color x:Key="PrimaryAccent">#1976D2</Color>        <!-- Blue 700 - Bleu principal -->
    <Color x:Key="PrimaryAccentLight">#42A5F5</Color>   <!-- Blue 400 -->
    <Color x:Key="PrimaryAccentDark">#1565C0</Color>    <!-- Blue 800 -->
    <Color x:Key="SecondaryAccent">#00897B</Color>      <!-- Teal 600 - Émeraude -->
    <Color x:Key="TertiaryAccent">#FF6F00</Color>       <!-- Orange 800 -->
    
    <!-- Mode Sombre (Élégant - Gris Foncé - NOUVEAU DESIGN SYSTEM) -->
    <Color x:Key="DarkBackground">#1E1E1E</Color>       <!-- Gris foncé -->
    <Color x:Key="DarkSurface">#1E1E1E</Color>          <!-- Gris foncé -->
    <Color x:Key="DarkCard">#2D2D2D</Color>             <!-- Gris moyen -->
    <Color x:Key="DarkCardElevated">#383838</Color>     <!-- Gris clair -->
    <Color x:Key="DarkTextPrimary">#FFFFFF</Color>      <!-- Blanc -->
    <Color x:Key="DarkTextSecondary">#B3B3B3</Color>    <!-- Gris clair -->
    <Color x:Key="DarkBorder">#404040</Color>           <!-- Gris bordure -->
    <Color x:Key="DarkBorderSubtle">#4A4A4A</Color>     <!-- Gris bordure clair -->

    
    <!-- Mode Clair (Style Financier - Très Clair) -->
    <Color x:Key="LightBackground">#F5F7FA</Color>      <!-- Gris très clair -->
    <Color x:Key="LightSurface">#FFFFFF</Color>         <!-- Blanc pur -->
    <Color x:Key="LightCard">#FFFFFF</Color>            <!-- Blanc pur -->
    <Color x:Key="LightCardElevated">#FFFFFF</Color>    <!-- Blanc pur -->
    <Color x:Key="LightTextPrimary">#1F2937</Color>     <!-- Gray 800 -->
    <Color x:Key="LightTextSecondary">#6B7280</Color>   <!-- Gray 500 -->
    <Color x:Key="LightBorder">#E5E7EB</Color>          <!-- Gray 200 -->
    <Color x:Key="LightBorderSubtle">#F3F4F6</Color>    <!-- Gray 100 -->
    
    <!-- Couleurs Fonctionnelles (NOUVEAU DESIGN SYSTEM) -->
    <Color x:Key="SuccessColor">#4CAF50</Color>         <!-- Green 500 -->
    <Color x:Key="WarningColor">#FF9800</Color>         <!-- Orange 500 -->
    <Color x:Key="ErrorColor">#F44336</Color>           <!-- Red 500 -->
    <Color x:Key="InfoColor">#2196F3</Color>            <!-- Blue 500 -->
    
    <!-- ============================================ -->
    <!-- BRUSHES DYNAMIQUES                           -->
    <!-- ============================================ -->
    
    <!-- Fonds -->
    <SolidColorBrush x:Key="AppBackgroundBrush" Color="{StaticResource LightBackground}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource LightSurface}"/>
    <SolidColorBrush x:Key="CardBrush" Color="{StaticResource LightCard}"/>
    <SolidColorBrush x:Key="CardElevatedBrush" Color="{StaticResource LightCardElevated}"/>
    <SolidColorBrush x:Key="GlassSurfaceBrush" Color="#DDFFFFFF"/>
    
    <!-- Accents -->
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryAccent}"/>
    <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryAccent}"/>
    <SolidColorBrush x:Key="TertiaryBrush" Color="{StaticResource TertiaryAccent}"/>
    
    <!-- Texte -->
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource LightTextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource LightTextSecondary}"/>
    
    <!-- Bordures -->
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource LightBorder}"/>
    <SolidColorBrush x:Key="BorderSubtleBrush" Color="{StaticResource LightBorderSubtle}"/>
    <SolidColorBrush x:Key="GlassBorderBrush" Color="{StaticResource LightBorder}"/>
    
    <!-- Dégradés Modernes -->
    <LinearGradientBrush x:Key="PrimaryGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource PrimaryAccent}" Offset="0"/>
        <GradientStop Color="{StaticResource SecondaryAccent}" Offset="1"/>
    </LinearGradientBrush>
    
    <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="{StaticResource PrimaryAccent}" Offset="0"/>
        <GradientStop Color="{StaticResource TertiaryAccent}" Offset="1"/>
    </LinearGradientBrush>
    
    <!-- Couleurs Fonctionnelles -->
    <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
    <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
    <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
    <SolidColorBrush x:Key="InfoBrush" Color="{StaticResource InfoColor}"/>
    
    <!-- ============================================ -->
    <!-- STYLES MODERNES & PROFESSIONNELS             -->
    <!-- ============================================ -->

    <!-- Card Moderne avec Ombre Douce (Style Médical) -->
    <Style x:Key="GlassCard" TargetType="Border">
        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="CornerRadius" Value="20"/>
        <Setter Property="Padding" Value="28"/>
        <Setter Property="Effect">
            <Setter.Value>
                <DropShadowEffect Color="#10000000" BlurRadius="30" ShadowDepth="0" Opacity="0.12"/>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Bouton Principal Moderne -->
    <Style x:Key="ModernButton" TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="20,12"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border" 
                            Background="{TemplateBinding Background}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <Border.Effect>
                            <DropShadowEffect Color="#30000000" BlurRadius="12" ShadowDepth="0" Opacity="0.15"/>
                        </Border.Effect>
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox Moderne -->
    <Style x:Key="ModernTextBox" TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                    Focusable="false" 
                                    HorizontalScrollBarVisibility="Hidden" 
                                    VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- PasswordBox Moderne -->
    <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
        <Setter Property="Background" Value="{DynamicResource CardBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="PasswordBox">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10"
                            Padding="{TemplateBinding Padding}">
                        <ScrollViewer x:Name="PART_ContentHost" 
                                    Focusable="false" 
                                    HorizontalScrollBarVisibility="Hidden" 
                                    VerticalScrollBarVisibility="Hidden"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource PrimaryBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
