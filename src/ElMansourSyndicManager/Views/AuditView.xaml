<UserControl x:Class="ElMansourSyndicManager.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewModels="clr-namespace:ElMansourSyndicManager.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance viewModels:AuditViewModel}">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="*"/>    <!-- DataGrid (takes remaining space) -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,32">
            <StackPanel>
                <TextBlock Text="Journal d'Audit" FontSize="28" FontWeight="Bold" Foreground="#333"/>
                <TextBlock Text="Suivi des activités et modifications" FontSize="16" Foreground="#777" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <Button Command="{Binding LoadCommand}" 
                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Content="Actualiser" 
                        Margin="0,0,12,0"
                        Height="36"/>
                <Button Command="{Binding ExportCommand}" 
                        Style="{StaticResource MaterialDesignRaisedButton}" 
                        Content="Exporter"
                        Height="36"/>
            </StackPanel>
        </Grid>

        <!-- Filters Card -->
        <materialDesign:Card Grid.Row="1" UniformCornerRadius="12" Background="White" Margin="0,0,0,16" Padding="20">
            <StackPanel Orientation="Horizontal">
                <DatePicker SelectedDate="{Binding FilterStartDate}" 
                            materialDesign:HintAssist.Hint="Date de début" 
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}" 
                            Width="150" 
                            Margin="0,0,16,0"/>
                <DatePicker SelectedDate="{Binding FilterEndDate}" 
                            materialDesign:HintAssist.Hint="Date de fin" 
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}" 
                            Width="150" 
                            Margin="0,0,16,0"/>
                <TextBox Text="{Binding FilterUserId}" 
                            materialDesign:HintAssist.Hint="ID Utilisateur" 
                            Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                            Width="150" 
                            Margin="0,0,16,0"/>
                <Button Command="{Binding FilterCommand}" 
                        Style="{StaticResource MaterialDesignOutlinedButton}" 
                        Content="Filtrer"
                        Height="36"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- DataGrid Card -->
        <materialDesign:Card Grid.Row="2" UniformCornerRadius="12" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Border Grid.Row="0" BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="20">
                    <TextBlock Text="Historique des Activités" FontWeight="SemiBold" FontSize="18" Foreground="#333"/>
                </Border>
                
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding Logs}" 
                          AutoGenerateColumns="False" 
                          CanUserAddRows="False" 
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          BorderThickness="0"
                          Background="White"
                          RowBackground="White"
                          AlternatingRowBackground="#F9FAFB"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#E5E7EB"
                          Padding="0,8"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Timestamp, StringFormat={}{0:dd/MM/yyyy HH:mm:ss}}" Width="160"/>
                        <DataGridTextColumn Header="Utilisateur" Binding="{Binding UserId}" Width="120"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="120"/>
                        <DataGridTextColumn Header="Type d'entité" Binding="{Binding EntityType}" Width="120"/>
                        <DataGridTextColumn Header="Détails" Binding="{Binding Details}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Loading Overlay -->
        <Grid Grid.RowSpan="3" Background="#80000000" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}" Value="0" IsIndeterminate="True" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Grid>
    </Grid>
</UserControl>
